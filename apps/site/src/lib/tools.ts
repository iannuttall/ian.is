/**
 * Public tools listed on /tools and, once there is at least one, in the
 * header nav. Empty until the first tool ships.
 */
export type Tool = {
  title: string;
  href: string;
  description: string;
};

export const tools: Tool[] = [];
