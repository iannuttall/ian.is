---
import type { HTMLAttributes } from 'astro/types'

type SelectOptionProps = HTMLAttributes<'option'>;

const { ...props } = Astro.props as SelectOptionProps
---
<option data-slot='select-option' {...props}>
    <slot />
  </option>
