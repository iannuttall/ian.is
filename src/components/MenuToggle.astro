---
interface Props {
  size?: number;
  /** id of the element this button expands (for aria-controls). */
  controls?: string;
  class?: string;
}
const { size = 24, controls, class: className } = Astro.props;
---

<button
  type="button"
  class:list={[
    "group/menu inline-flex items-center justify-center text-foreground",
    "transition-colors focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring focus-visible:ring-offset-2 focus-visible:ring-offset-background",
    className,
  ]}
  x-data="menuToggle"
  x-on:click="toggle"
  aria-label="Toggle menu"
  aria-expanded="false"
  x-bind:aria-expanded="String(open)"
  aria-controls={controls}
>
  <svg
    width={size}
    height={size}
    viewBox="0 0 24 24"
    color="currentColor"
    fill="none"
    stroke="currentColor"
    stroke-width="1.5"
    stroke-linecap="round"
    stroke-linejoin="round"
    class="overflow-visible"
    aria-hidden="true"
  >
    <path
      d="M4 9L20 9"
      class="[transform-box:view-box] origin-[12px_9px] transition-transform duration-200 ease-in-out motion-reduce:transition-none group-aria-expanded/menu:[transform:translateY(3px)_rotate(45deg)]"
    />
    <path
      d="M4 15.5L14 15.5"
      class="[transform-box:view-box] origin-[9px_15.5px] transition-transform duration-200 ease-in-out motion-reduce:transition-none group-aria-expanded/menu:[transform:translate(3px,-3.5px)_rotate(-45deg)_scaleX(1.6)]"
    />
  </svg>
</button>
